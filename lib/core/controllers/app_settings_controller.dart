// -----------------------------------------------------------------------------
// DEPRECATED — superseded by Riverpod in Phase 10.
//
// The Phase 9 ChangeNotifier singleton that previously lived here has been
// replaced by `AppSettings` + `settingsProvider` in:
//
//     lib/core/providers/settings_provider.dart
//
// This file is intentionally left empty. It is kept only because the sandbox
// does not permit file deletion — it contributes no symbols to the build and
// is not imported anywhere in the project.
// -----------------------------------------------------------------------------
